// #ifndef ENEMYGHOST_H_INCLUDED
// #define ENEMYGHOST_H_INCLUDED

// #include "Enemy.h"

// // fixed settings: MonsterCaveMan attributes
// class EnemyGhost : public Enemy
// {
// public:
// 	EnemyGhost(const std::vector<Point> &path) : Enemy{path, EnemyType::GHOST} {
// 		v = 40;
// 		//bitmap_img_ids.emplace_back(std::vector<int>({0})); // UP
// 		// bitmap_img_ids.emplace_back(std::vector<int>({0, 1, 2, 3})); // DOWN
// 		// bitmap_img_ids.emplace_back(std::vector<int>({0, 1, 2, 3})); // LEFT
// 		// bitmap_img_ids.emplace_back(std::vector<int>({0, 1, 2, 3})); // RIGHT
// 		// bitmap_switch_freq = 20;
// 	}
// };

// #endif
